### Added

- Added `OPENPOST_ANALYTICS_SOURCES` for operator-configured per-platform external analytics collectors, with `_FILE` support, startup validation, analytics-only registration, source-backed request and error mapping, and docs for the JSON contract and collector responses.
