### Fixed

- Kept connected accounts healthy when OAuth refresh fails because of a provider rate limit, server error, or timeout.
- Blocked X videos outside the provider's supported aspect-ratio range before delivery.
