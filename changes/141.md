fix: resume the Video Editor render queue after reload

Queued exports now continue automatically after reopening a project when the queue was running before reload. Interrupted renders still return in a safe paused state.
